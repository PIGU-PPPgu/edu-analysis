import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.education.management',
  appName: 'Grade Analysis System',
  webDir: 'dist'
};

export default config;
