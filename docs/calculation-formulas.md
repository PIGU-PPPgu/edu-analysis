# 增值评价计算公式完整说明

**版本**: v1.0
**更新日期**: 2026-02-10
**维护者**: 教育管理系统开发团队

---

## 📋 目录

1. [概述](#概述)
2. [基础统计公式](#基础统计公式)
3. [增值评价核心公式](#增值评价核心公式)
4. [等级划分体系](#等级划分体系)
5. [边界情况处理](#边界情况处理)
6. [代码实现参考](#代码实现参考)
7. [常见问题FAQ](#常见问题faq)

---

## 概述

### 什么是增值评价？

增值评价（Value-Added Assessment）是一种教育评价方法，通过比较学生的**入口成绩**（如学期初）和**出口成绩**（如学期末），评估教师、班级或学校对学生学业进步的**贡献程度**。

与传统的绝对分数评价不同，增值评价关注的是**相对进步**，更公平地反映教学效果。

### 核心理念

1. **公平性**：考虑学生起点差异，低起点学生的大幅进步同样值得肯定
2. **科学性**：使用标准化统计方法（Z-score）消除试卷难度影响
3. **全面性**：从多个维度评价（分数增值、等级提升、优秀人数贡献）

---

## 基础统计公式

### 1. 样本标准差（Sample Standard Deviation）

**公式**：

```
σ = sqrt(Σ(xᵢ - μ)² / (n - 1))
```

**参数说明**：
- `σ` - 样本标准差
- `xᵢ` - 第i个样本值
- `μ` - 样本均值
- `n` - 样本数量

**为什么用 n-1？**

使用 `n-1`（贝塞尔修正）而非 `n`，提供**无偏估计**。当样本量较小时，这个修正尤为重要。

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:23-35
export function calculateStandardDeviation(values: number[]): number {
  if (values.length === 0) return 0;
  if (values.length === 1) return 0; // 单个样本标准差为0

  const mean = values.reduce((sum, val) => sum + val, 0) / values.length;
  const squaredDiffs = values.map((val) => Math.pow(val - mean, 2));

  // 使用n-1（样本标准差）
  const variance = squaredDiffs.reduce((sum, val) => sum + val, 0) / (values.length - 1);

  return Math.sqrt(variance);
}
```

**示例**：

学生分数：`[100, 90, 80, 70, 60]`

1. 计算均值：μ = (100+90+80+70+60)/5 = 80
2. 计算偏差平方和：(100-80)² + (90-80)² + (80-80)² + (70-80)² + (60-80)² = 1000
3. 除以 n-1：1000/(5-1) = 250
4. 开平方：σ = √250 ≈ 15.81

---

### 2. Z-Score（标准分）

**公式**：

```
Z = (X - μ) / σ
```

**参数说明**：
- `Z` - Z分数（标准分）
- `X` - 原始分数
- `μ` - 该组数据的均值
- `σ` - 该组数据的标准差

**用途**：

将不同难度的考试分数**标准化**，使其可以横向比较。

**解读**：
- `Z = 0`：分数等于平均分
- `Z = 1`：分数比平均分高1个标准差（优秀）
- `Z = -1`：分数比平均分低1个标准差（需提升）
- `Z > 2`：非常优秀（前2.5%）
- `Z < -2`：需要重点关注（后2.5%）

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:40-47
export function calculateZScore(
  value: number,
  mean: number,
  stdDev: number
): number {
  if (stdDev === 0) return 0; // 避免除以0
  return (value - mean) / stdDev;
}
```

**示例**：

考试A：平均分70，标准差10，小明得分80
→ Z = (80-70)/10 = **1.0**

考试B：平均分60，标准差15，小明得分75
→ Z = (75-60)/15 = **1.0**

**结论**：虽然绝对分数不同，但小明在两次考试中的**相对表现一致**。

---

### 3. 百分位（Percentile）

**公式**：

```
百分位 = (rank - 1) / (n - 1)
```

**参数说明**：
- `rank` - 升序排名（从1开始）
- `n` - 总人数

**语义**：
- **百分位 = 1.0**：分数最高（超过100%的人）
- **百分位 = 0.5**：中位数（超过50%的人）
- **百分位 = 0.0**：分数最低（超过0%的人）

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:152-177
export function calculatePercentile(
  value: number,
  allValues: number[]
): number {
  if (allValues.length === 0) return 0;
  if (allValues.length === 1) return 1;

  // 升序排列：低分在前，高分在后
  const sortedValues = [...allValues].sort((a, b) => a - b);

  // 找到第一个大于等于value的位置
  let rank = sortedValues.findIndex((v) => v >= value);
  if (rank === -1) rank = sortedValues.length - 1;
  rank = rank + 1; // 转换为从1开始的排名

  return (rank - 1) / (allValues.length - 1);
}
```

**示例**：

班级10人，分数（升序）：`[60, 65, 70, 75, 80, 85, 90, 95, 100, 105]`

- 小明得分90：rank=7，百分位 = (7-1)/(10-1) = 6/9 ≈ **0.67**（超过67%的同学）
- 小红得分105：rank=10，百分位 = (10-1)/(10-1) = 9/9 = **1.0**（最高分）

---

### 4. 中位数（Median）和四分位数（Quartiles）

**中位数公式**：

```
如果 n 为奇数：median = sorted[n/2]
如果 n 为偶数：median = (sorted[n/2-1] + sorted[n/2]) / 2
```

**四分位数**：

- **Q1（第一四分位数）**：下半部分的中位数（25th percentile）
- **Q2（第二四分位数）**：整体中位数（50th percentile）
- **Q3（第三四分位数）**：上半部分的中位数（75th percentile）

**用途**：

用于**箱线图（Box Plot）**，识别离群值和数据分布。

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:78-97
export function calculateQuartiles(values: number[]): {
  q1: number;
  q2: number;
  q3: number;
} {
  if (values.length === 0) return { q1: 0, q2: 0, q3: 0 };

  const sorted = [...values].sort((a, b) => a - b);
  const q2 = calculateMedian(sorted);

  const mid = Math.floor(sorted.length / 2);
  const lowerHalf = sorted.slice(0, mid);
  const upperHalf = sorted.length % 2 === 0 ? sorted.slice(mid) : sorted.slice(mid + 1);

  const q1 = calculateMedian(lowerHalf);
  const q3 = calculateMedian(upperHalf);

  return { q1, q2, q3 };
}
```

---

## 增值评价核心公式

### 5. 分数增值率（Score Value-Added Rate）

**公式**：

```
增值率 = 出口Z分数 - 入口Z分数
```

**参数说明**：
- **入口Z分数**：学生在学期初考试的标准分
- **出口Z分数**：学生在学期末考试的标准分

**解读**：
- **增值率 > 0**：学生进步了（出口表现优于入口）
- **增值率 = 0**：学生维持原水平
- **增值率 < 0**：学生退步了（出口表现弱于入口）

**为什么不用原始分数的差值？**

不同考试难度不同，直接比较原始分数不公平。使用Z-score标准化后，消除了试卷难度的影响。

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:316-323
export function calculateScoreValueAddedRate(
  entryZScore: number,
  exitZScore: number
): number {
  // 直接使用Z-score差值
  return exitZScore - entryZScore;
}
```

**示例**：

**学生A**：
- 入口考试：70分（班级均分60，标准差10）→ Z = (70-60)/10 = **1.0**
- 出口考试：85分（班级均分80，标准差10）→ Z = (85-80)/10 = **0.5**
- 增值率 = 0.5 - 1.0 = **-0.5**（退步）

**学生B**：
- 入口考试：50分（班级均分60，标准差10）→ Z = (50-60)/10 = **-1.0**
- 出口考试：75分（班级均分80，标准差10）→ Z = (75-80)/10 = **-0.5**
- 增值率 = -0.5 - (-1.0) = **+0.5**（进步）

**结论**：虽然学生B的绝对分数低于A，但B的相对进步更大，教学效果更好。

---

### 6. 巩固率（Consolidation Rate）

**公式**：

```
巩固率 = 保持A+等级的学生数 / 入口A+等级的学生总数
```

**用途**：

评估教师对**优秀学生的保持能力**。

**解读**：
- **巩固率 = 100%**：所有优秀学生都保持在顶尖水平
- **巩固率 < 100%**：部分优秀学生出现退步

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:371-383
export function calculateConsolidationRate(
  students: Array<{ entryLevel: AbilityLevel; exitLevel: AbilityLevel }>
): number {
  const highestLevelStudents = students.filter((s) => s.entryLevel === "A+");

  if (highestLevelStudents.length === 0) return 0;

  const consolidatedCount = highestLevelStudents.filter(
    (s) => s.exitLevel === "A+"
  ).length;

  return consolidatedCount / highestLevelStudents.length;
}
```

**示例**：

班级30人，入口A+等级10人：
- 出口仍为A+：8人
- 出口降为A或更低：2人
- **巩固率 = 8/10 = 80%**

---

### 7. 转化率（Transformation Rate）

**公式**：

```
转化率 = 等级提升的学生数 / 可提升学生总数
```

**参数说明**：
- **可提升学生**：入口等级非A+的学生（有提升空间）
- **等级提升**：出口等级 > 入口等级（如B→B+, B+→A）

**用途**：

评估教师对**中下游学生的提升能力**。

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:389-402
export function calculateTransformationRate(
  students: Array<{ entryLevel: AbilityLevel; exitLevel: AbilityLevel }>
): number {
  // 可提升学生：入口不是最高等级的学生
  const improvableStudents = students.filter((s) => s.entryLevel !== "A+");

  if (improvableStudents.length === 0) return 0;

  const transformedCount = improvableStudents.filter((s) =>
    isTransformed(s.entryLevel, s.exitLevel)
  ).length;

  return transformedCount / improvableStudents.length;
}
```

**示例**：

班级30人，入口A+等级10人，可提升学生20人：
- 等级提升：12人（B→A, C→B+等）
- 等级不变或下降：8人
- **转化率 = 12/20 = 60%**

---

### 8. 贡献率（Contribution Rate）

**公式**：

**正常情况（年级优秀人数上升）**：

```
贡献率 = 该教师优秀人数增量 / 年级优秀人数增量
```

**特殊情况（年级优秀人数下降，但该教师上升）**：

```
贡献率 = |该教师优秀人数增量 / 年级优秀人数增量|（逆势增长）
```

**参数说明**：
- **优秀人数增量**：出口优秀人数 - 入口优秀人数
- **优秀等级**：通常指A+和A等级

**用途**：

评估教师对**年级整体优秀人数增长的贡献**。

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:416-430
export function calculateContributionRate(
  teacherExcellentGain: number,
  gradeExcellentGain: number
): number {
  if (gradeExcellentGain === 0) return 0;

  // 处理年级下降但教师上升的情况（逆势增长）
  if (gradeExcellentGain < 0 && teacherExcellentGain > 0) {
    return Math.abs(teacherExcellentGain / gradeExcellentGain);
  }

  // 正常情况
  return teacherExcellentGain / gradeExcellentGain;
}
```

**示例**：

**正常情况**：
- 年级入口优秀人数：100人
- 年级出口优秀人数：120人
- 年级增量：+20人
- 张老师班级优秀人数增量：+8人
- **贡献率 = 8/20 = 40%**

**逆势增长**：
- 年级入口优秀人数：100人
- 年级出口优秀人数：90人
- 年级增量：-10人（整体下降）
- 李老师班级优秀人数增量：+5人（逆势上升）
- **贡献率 = |5/(-10)| = 50%**（标注为"逆势增长"）

---

### 9. 进步人数占比（Progress Ratio）

**公式**：

```
进步人数占比 = 出口分数 > 入口分数的学生数 / 总学生数
```

**用途**：

简单直观地反映班级的**整体进步情况**。

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:328-344
export function calculateProgressRatio(
  entryScores: number[],
  exitScores: number[]
): number {
  if (entryScores.length === 0 || entryScores.length !== exitScores.length) {
    return 0;
  }

  let progressCount = 0;
  for (let i = 0; i < entryScores.length; i++) {
    if (exitScores[i] > entryScores[i]) {
      progressCount++;
    }
  }

  return progressCount / entryScores.length;
}
```

**示例**：

班级30人：
- 分数提高：20人
- 分数不变或降低：10人
- **进步人数占比 = 20/30 ≈ 66.7%**

---

## 等级划分体系

### 10. 能力等级定义（Ability Level）

基于**百分位**划分6个等级：

| 等级 | 百分位范围 | 说明 | 数值映射 |
|------|------------|------|----------|
| **A+** | ≥ 0.90 | 优秀（前10%） | 6 |
| **A** | 0.75 ~ 0.90 | 良好（前25%） | 5 |
| **B+** | 0.60 ~ 0.75 | 中上 | 4 |
| **B** | 0.40 ~ 0.60 | 中等 | 3 |
| **C+** | 0.25 ~ 0.40 | 中下 | 2 |
| **C** | < 0.25 | 需提升（后25%） | 1 |

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:223-235
export function determineLevel(
  percentile: number,
  levelDefinitions: GradeLevelDefinition[]
): AbilityLevel {
  for (const def of levelDefinitions) {
    if (percentile >= def.percentile.min && percentile < def.percentile.max) {
      return def.level;
    }
  }
  return "C"; // 默认最低等级
}
```

**等级数值映射**（用于比较和计算等级变化）：

```typescript
// 文件：src/utils/statistics.ts:253-264
export function getLevelValue(level: AbilityLevel): number {
  const levelMap: Record<AbilityLevel, number> = {
    "A+": 6,
    A: 5,
    "B+": 4,
    B: 3,
    "C+": 2,
    C: 1,
  };
  return levelMap[level] || 0;
}
```

**等级变化计算**：

```typescript
// 文件：src/utils/statistics.ts:297-302
export function calculateLevelChange(
  entryLevel: AbilityLevel,
  exitLevel: AbilityLevel
): number {
  return getLevelValue(exitLevel) - getLevelValue(entryLevel);
}
```

**示例**：

- 入口等级：B（值=3）
- 出口等级：A（值=5）
- **等级变化 = 5 - 3 = +2**（提升2个等级）

---

### 11. 优秀人数净增加

**公式**：

```
优秀人数净增加 = 出口优秀人数 - 入口优秀人数
```

**参数说明**：
- **优秀等级**：默认为A+和A
- 可根据实际需求调整优秀标准

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:420-434
export function calculateExcellentGain(
  entryLevels: AbilityLevel[],
  exitLevels: AbilityLevel[],
  excellentLevels: AbilityLevel[] = ["A+", "A"]
): number {
  const entryExcellentCount = entryLevels.filter((level) =>
    excellentLevels.includes(level)
  ).length;

  const exitExcellentCount = exitLevels.filter((level) =>
    excellentLevels.includes(level)
  ).length;

  return exitExcellentCount - entryExcellentCount;
}
```

---

### 12. 学科偏离度（Subject Balance Score）

**公式**：

```
学科偏离度 = 各科目增值率的标准差
学科均衡得分 = w1 × 总分增值率 - w2 × 学科偏离度
```

**参数说明**：
- `w1` - 总分增值权重（默认0.6）
- `w2` - 偏离度权重（默认0.4）

**用途**：

评估学生或班级的**学科发展均衡性**。

**解读**：
- **偏离度小**：各科目发展均衡
- **偏离度大**：存在严重偏科

**代码实现**：

```typescript
// 文件：src/utils/statistics.ts:444-465
export function calculateSubjectDeviation(
  subjectValueAddedRates: number[]
): number {
  return calculateStandardDeviation(subjectValueAddedRates);
}

export function calculateSubjectBalanceScore(
  totalValueAdded: number,
  subjectDeviation: number,
  w1: number = 0.6,
  w2: number = 0.4
): number {
  // 偏离度越小越好，所以用负数
  return w1 * totalValueAdded - w2 * subjectDeviation;
}
```

**示例**：

学生各科目增值率：`[0.5, 0.6, 0.4, 0.8, 0.3]`

1. 计算总分增值率：0.52（假设）
2. 计算学科偏离度：σ ≈ 0.18
3. 学科均衡得分 = 0.6×0.52 - 0.4×0.18 ≈ **0.24**

---

## 边界情况处理

### 1. 空数据集

**问题**：数组为空时如何处理？

**解决**：
- 标准差、中位数、百分位等统计量返回0
- Z-score返回0（避免除以0）

```typescript
if (values.length === 0) return 0;
```

### 2. 单个样本

**问题**：只有1个数据点时，标准差是多少？

**解决**：
- 样本标准差为0（无离散度）
- 百分位为1.0（唯一值即为最大值）

```typescript
if (values.length === 1) return 0; // 标准差
if (values.length === 1) return 1; // 百分位
```

### 3. 标准差为0

**问题**：所有值相同时（如全班都得100分），标准差为0，Z-score无法计算。

**解决**：
- 检测到σ=0时，返回Z=0（表示无差异）

```typescript
if (stdDev === 0) return 0;
```

### 4. 年级优秀人数下降

**问题**：年级整体优秀人数减少，但个别教师增加，如何计算贡献率？

**解决**：
- 使用绝对值，并标注为"逆势增长"
- 正值表示正向贡献

```typescript
if (gradeExcellentGain < 0 && teacherExcellentGain > 0) {
  return Math.abs(teacherExcellentGain / gradeExcellentGain);
}
```

### 5. 入口Z分数极低

**问题**：学生入口Z分数 < -5时，旧公式会导致负数分母。

**解决**：
- ✅ **已修复**：直接使用Z-score差值，避免标准分转换

```typescript
// 旧公式（有问题）：
return (50 + 10*exitZ - 50 - 10*entryZ) / (50 + 10*entryZ)

// 新公式（已修复）：
return exitZScore - entryZScore;
```

---

## 代码实现参考

### 主要文件

1. **`src/utils/statistics.ts`** - 核心统计计算函数
   - 标准差、Z-score、百分位、四分位数
   - 增值率、巩固率、转化率、贡献率

2. **`src/services/ai/statisticalAnalysis.ts`** - AI分析辅助
   - 异常值检测（Z-score、IQR方法）
   - 数据分布分析
   - 趋势预测

3. **`src/types/valueAddedTypes.ts`** - 类型定义
   - Statistics, PercentileData, AbilityLevel
   - GradeLevelDefinition, ValueAddedMetrics

### 单元测试

**文件**：`src/utils/__tests__/statistics.algorithm-fixes.test.ts`

验证所有核心算法的正确性：
- ✅ 增值率公式（Z-score差值）
- ✅ 百分位语义（高分=高百分位）
- ✅ 贡献率负值处理
- ✅ 样本标准差（n-1公式）

---

## 常见问题FAQ

### Q1: 为什么要使用Z-score而不是原始分数？

**A**: 不同考试难度不同，直接比较原始分数不公平。Z-score将分数标准化到相同的尺度（均值=0，标准差=1），使得不同考试的成绩可以横向比较。

**示例**：
- 简单考试：平均80分，小明90分
- 困难考试：平均60分，小明70分

虽然绝对分数不同，但小明的Z-score都是1.0（假设标准差10），说明相对表现一致。

---

### Q2: 增值率为负数是否意味着教师教学不好？

**A**: 不一定。增值率为负可能有多种原因：
1. 学生基础较好，入口Z-score已经很高，难以继续提升
2. 试卷难度增加，导致整体水平下降
3. 学生学习态度变化
4. 教师确实需要改进教学方法

需要**结合多次考试趋势**和**班级整体情况**综合判断。

---

### Q3: 巩固率和转化率哪个更重要？

**A**: 两者评价不同维度，都很重要：

- **巩固率**：评价教师对**优秀学生的保持能力**
  - 适用于重点班、实验班
  - 防止"吃老本"现象

- **转化率**：评价教师对**中下游学生的提升能力**
  - 适用于普通班、后进班
  - 体现教育公平性

**建议**：两者结合使用，全面评价教学效果。

---

### Q4: 为什么百分位公式用 (n-1) 而不是 n？

**A**: 这是**标准的百分位计算方法**（线性插值法）：

- 使用 (n-1)：百分位范围为 [0, 1]
  - 最小值：percentile = 0
  - 最大值：percentile = 1

- 使用 n：百分位范围为 [0, (n-1)/n]
  - 最大值：percentile = 0.9（10人时）
  - 语义不直观

---

### Q5: 如何处理学科偏离度过大的学生？

**A**: 学科偏离度大表示严重偏科，建议：

1. **诊断原因**：
   - 学习兴趣差异
   - 基础薄弱科目
   - 教师教学方式不适应

2. **干预措施**：
   - 弱科补习
   - 学习方法指导
   - 兴趣培养

3. **跟踪评估**：
   - 定期计算学科偏离度
   - 观察干预效果

---

### Q6: 贡献率超过100%是否合理？

**A**: **完全合理**。贡献率 > 100% 表示该教师的优秀人数增量**超过了年级总增量**，说明：

1. 该教师班级优秀人数大幅增加
2. 其他班级可能有所下降
3. 该教师对年级优秀人数增长起到**主要贡献**

**示例**：
- 年级优秀人数增量：+10人
- 张老师班级优秀人数增量：+15人
- 贡献率 = 15/10 = **150%**

---

### Q7: 如何验证计算公式的正确性？

**A**: 参考单元测试文件：

```bash
npx vitest run src/utils/__tests__/statistics.algorithm-fixes.test.ts
```

包含16个测试用例，覆盖所有核心公式的边界情况。

---

## 附录：术语对照表

| 中文术语 | 英文术语 | 公式符号 |
|----------|---------|---------|
| 样本标准差 | Sample Standard Deviation | σ |
| Z分数/标准分 | Z-Score | Z |
| 百分位 | Percentile | P |
| 四分位数 | Quartile | Q1, Q2, Q3 |
| 增值率 | Value-Added Rate | VAR |
| 巩固率 | Consolidation Rate | CR |
| 转化率 | Transformation Rate | TR |
| 贡献率 | Contribution Rate | CTR |
| 学科偏离度 | Subject Deviation | SD |
| 均值 | Mean | μ (mu) |
| 方差 | Variance | σ² |

---

## 修订历史

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|---------|--------|
| v1.0 | 2026-02-10 | 初始版本，包含12个核心公式 | Claude |

---

**维护说明**：

本文档随代码同步更新。如修改计算公式，请同步更新：
1. 本文档的公式说明
2. 代码注释
3. 单元测试用例
4. UI组件展示（`FormulaExplanation.tsx`）
