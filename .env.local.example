# ====================================
# 🧪 测试环境配置示例
# ====================================
#
# 说明:
# 1. 复制此文件为 `.env.local`
# 2. 填入实际的 Supabase 项目配置
# 3. 测试环境使用 test_schema 隔离数据
# 4. 生产环境保持使用 public schema
#
# 创建时间: 2024-11-30
# ====================================

# ====================================
# 📌 生产环境配置
# ====================================

# Supabase项目URL（从 https://app.supabase.com 获取）
VITE_SUPABASE_URL=https://giluhqotfjpmofowvogn.supabase.co

# Supabase匿名密钥（从项目设置获取）
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# ====================================
# 🧪 测试环境配置（新增）
# ====================================

# 测试环境使用相同的Supabase项目
VITE_TEST_SUPABASE_URL=https://giluhqotfjpmofowvogn.supabase.co

# 测试环境使用相同的匿名密钥
VITE_TEST_SUPABASE_ANON_KEY=your_anon_key_here

# 🆕 测试环境schema配置（关键配置！）
# 通过schema隔离实现测试/生产数据分离
VITE_TEST_SUPABASE_SCHEMA=test_schema

# ====================================
# 📝 配置说明
# ====================================
#
# 数据隔离策略:
# - 生产环境: 使用 public schema (默认)
# - 测试环境: 使用 test_schema (通过VITE_TEST_SUPABASE_SCHEMA配置)
#
# 优势:
# 1. 无需创建单独的测试Supabase项目
# 2. 测试数据与生产数据完全隔离
# 3. 可共享相同的API密钥和认证配置
# 4. 测试环境使用宽松的RLS策略
#
# 运行测试前请确保:
# 1. 已运行 `supabase/migrations/20251130_create_test_schema.sql`
# 2. test_schema 已成功创建并设置权限
# 3. 已复制此文件为 `.env.local` 并填入实际配置
#
# ====================================
